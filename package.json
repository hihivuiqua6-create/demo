{
  "name": "ddos-load-test-server",
  "version": "1.0.0",
  "description": "Load testing server with proxy rotation and multi-worker simulation (FOR OWN SERVER TESTING ONLY)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "eslint . --ext .js"
  },
  "keywords": [
    "load-test",
    "stress-test",
    "proxy-rotation",
    "ddos-simulation",
    "testing-tool"
  ],
  "author": "minh",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "worker_threads": "^1.0.0"  // built-in, nhưng liệt kê để rõ
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "eslint": "^9.9.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
